apiVersion: v1
kind: Service
metadata:
  name: node-api-svc
  namespace: default
spec:
  selector:
    app: node-api
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000    